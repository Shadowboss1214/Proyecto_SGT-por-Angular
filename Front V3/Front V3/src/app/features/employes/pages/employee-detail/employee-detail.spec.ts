/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransportDetail } from './transport-detail';

describe('TransportDetail', () => {
  let component: TransportDetail;
  let fixture: ComponentFixture<TransportDetail>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [TransportDetail]
    })
    .compileComponents();

    fixture = TestBed.createComponent(TransportDetail);
    component = fixture.componentInstance;
    await fixture.whenStable();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/